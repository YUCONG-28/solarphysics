repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
        args: ['--maxkb=2000']

  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.0
    hooks:
      # Intentionally unscoped: secrets are checked across every public partition.
      - id: gitleaks

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.12
    hooks:
      - id: ruff
        files: ^(?:Python|Apps)/.*\.py$
        args:
          - --fix

  - repo: https://github.com/psf/black
    rev: v25.1.0
    hooks:
      - id: black
        files: ^(?:Python|Apps)/.*\.py$
        args:
          - --line-length=88
