# Compatibility/source specification only. This file is intentionally solved;
# it is not an exact environment lock. See ../environment/README.md.
name: solarphysics_env_latest
channels:
  - conda-forge
dependencies:
  - python=3.14
  - pip
