[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "solarphysics-apps"
dynamic = ["version"]
description = "Local-first frontends and reproducible workflows for solar physics"
readme = "README.md"
requires-python = ">=3.14"
license = "MIT"
dependencies = [
  "av>=16,<19",
  "Flask>=3.0",
  "openpyxl>=3.1",
  "Pillow>=10.0",
  "plotly>=5.18",
  "PyQt6==6.9.1",
  "PyQt6-Qt6==6.9.1",
  "PyYAML>=6.0",
  "opencv-python-headless>=5.0.0.93,<6",
  "pyqtgraph>=0.13",
  "streamlit>=1.58",
]

[project.optional-dependencies]
dev = [
  "black>=25.1.0,<26",
  "build>=1.2",
  "pre-commit>=4.0",
  "pytest>=8.0",
  "ruff>=0.9",
]

[tool.setuptools]
include-package-data = true

[tool.setuptools.dynamic]
version = {attr = "solar_apps._version.__version__"}

[tool.setuptools.packages.find]
where = ["."]
include = ["solar_apps*"]

[tool.setuptools.package-data]
solar_apps = [
  "**/*.cjs",
  "**/*.css",
  "**/*.html",
  "**/*.js",
  "**/*.json",
  "**/*.txt",
  "**/*.yaml",
  "**/*.yml",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-q -p no:cacheprovider"

[tool.ruff]
target-version = "py314"
line-length = 88

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]

[tool.black]
target-version = ["py314"]
line-length = 88
