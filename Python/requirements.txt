# Core scientific stack for the solar-physics workflows.
# The package metadata in pyproject.toml is the canonical install definition;
# this file is provided as a convenient environment checklist.

numpy>=1.24.0
scipy>=1.10.0
astropy>=5.3.0
sunpy[map,net]>=5.0.0
mpl-animators>=1.2.0
matplotlib>=3.7.0
reproject>=0.10.0
scikit-image>=0.21.0
pyyaml>=6.0.0
pandas>=2.0.0
tqdm>=4.65.0

# Frequently used by optional workflows.
xarray>=2023.6.0
netCDF4>=1.6.0
imageio>=2.31.0
pillow>=10.0.0
psutil>=5.9.0
requests>=2.31.0

# Workflow-specific optional packages:
# - hvpy: required only by SOHO/LASCO Helioviewer downloads.
hvpy>=1.1.0
# Application, GUI, browser, and event-workflow dependencies live in
# ../Local/requirements.local.txt and are not part of the public wheel.
